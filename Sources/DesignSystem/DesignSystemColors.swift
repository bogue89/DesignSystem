extension DesignSystem {

    public struct Colors {
        
        public let environment: DesingSystemEnvironment
        
        public init(_ environment: DesingSystemEnvironment) {
            self.environment = environment
        }
    }
}
